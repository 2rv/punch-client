const ROUTES = {
  HOME: '/',
  RESPONSE: '/response',
  DASHBOARD: '/dashboard',
  LOGIN: '/login',
};

export default ROUTES;

export const ANCHORS_ID = {};

export const HOME_MENU = [{ id: 2, tid: 'NAVIGATION.PATH.HOME', path: ROUTES.HOME }];

export const PAGINATION_OFFSET = {};
